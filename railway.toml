[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "java -Xmx512m -Xms256m -jar /opt/Lavalink/Lavalink.jar"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
